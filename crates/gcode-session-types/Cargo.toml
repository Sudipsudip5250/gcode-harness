[package]
name = "gcode-session-types"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
gcode-message-types = { path = "../gcode-message-types" }
serde = { version = "1", features = ["derive"] }
