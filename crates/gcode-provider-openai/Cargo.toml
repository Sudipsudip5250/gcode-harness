[package]
name = "gcode-provider-openai"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
gcode-message-types = { path = "../gcode-message-types" }
gcode-provider-core = { path = "../gcode-provider-core" }
serde_json = "1"
