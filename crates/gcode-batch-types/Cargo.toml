[package]
name = "gcode-batch-types"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
gcode-message-types = { path = "../gcode-message-types" }
serde = { version = "1", features = ["derive"] }
